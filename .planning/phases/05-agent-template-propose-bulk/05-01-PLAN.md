---
phase: 05-agent-template-propose-bulk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/onboarding-assets/default/AGENTS.md
autonomous: true
requirements:
  - AGENTS-01
must_haves:
  truths:
    - "A newly created agent's AGENTS.md contains documented Stapler tool names and brief descriptions"
    - "The template covers all 8 Stapler-specific tools: memory (save, search, list, delete) and goal (create, update, list) plus company memories"
  artifacts:
    - path: "server/src/onboarding-assets/default/AGENTS.md"
      provides: "Default instructions injected into new agents"
      contains: "paperclip_save_memory"
  key_links:
    - from: "server/src/services/default-agent-instructions.ts"
      to: "server/src/onboarding-assets/default/AGENTS.md"
      via: "fs.readFile at resolveDefaultAgentBundleUrl('default', 'AGENTS.md')"
      pattern: "loadDefaultAgentInstructionsBundle"
---

<objective>
Update the default AGENTS.md template so every new agent immediately knows which Stapler-specific tools are available and how to use them.

Purpose: Agents created today have no tool documentation in their system prompt. Adding it reduces trial-and-error and makes memory/goal tools discoverable without reading source code.
Output: Updated `server/src/onboarding-assets/default/AGENTS.md` with a Tools section listing all 8 Stapler tools with brief descriptions.
</objective>

<execution_context>
@/Users/dawid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- How the default template is loaded and applied -->
<!-- server/src/services/default-agent-instructions.ts:
     loadDefaultAgentInstructionsBundle('default') reads AGENTS.md from
     server/src/onboarding-assets/default/AGENTS.md via fs.readFile.
     This bundle is then passed into agent creation in server/src/routes/agents.ts
     (line ~583) and written as the agent's instructions file with replaceExisting: false. -->

<!-- Current file content (4 lines, no tool documentation): -->
<!-- You are an agent at Paperclip company.
     Keep the work moving until it's done... -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stapler Tools section to default AGENTS.md</name>
  <files>server/src/onboarding-assets/default/AGENTS.md</files>
  <action>
    Rewrite `server/src/onboarding-assets/default/AGENTS.md`. Keep the existing 4-line preamble intact, then append a "## Stapler Tools" section that documents all 8 Stapler-specific tools. For each tool include: tool name (as a code span), one-sentence description, and the key parameter(s).

    Tools to document:
    - `paperclip_save_memory` — Persist a note for future runs. Params: content (string), tags (string[]).
    - `paperclip_search_memories` — Semantic search across your saved memories. Params: query (string).
    - `paperclip_list_memories` — List all your saved memories, optionally filtered by tag. Params: tag? (string).
    - `paperclip_delete_memory` — Remove a memory by ID. Params: id (string).
    - `paperclip_list_company_memories` — List memories shared across all agents in this company. Params: limit? (number, default 50).
    - `paperclip_create_goal` — Create a new goal with a title and acceptance criteria. Params: title (string), acceptanceCriteria (string).
    - `paperclip_update_goal` — Update an existing goal's status or description. Params: id (string), status? ("open"|"in_progress"|"achieved"|"abandoned"), description? (string).
    - `paperclip_list_goals` — List all goals for this agent's company. Params: none required.

    Format example for each entry:
    ```
    ### `paperclip_save_memory`
    Persist a note you want to remember in future runs.
    **Params:** `content` (string), `tags` (string[])
    ```

    Do not add any other content or change the preamble.
  </action>
  <verify>
    <automated>grep -c "paperclip_" server/src/onboarding-assets/default/AGENTS.md | grep -q "^8$" && echo "PASS: 8 tools found" || echo "FAIL"</automated>
  </verify>
  <done>File contains all 8 tool entries. The preamble is unchanged. A new agent created after this change will receive tool documentation in its AGENTS.md.</done>
</task>

</tasks>

<verification>
grep "paperclip_save_memory\|paperclip_search_memories\|paperclip_list_memories\|paperclip_delete_memory\|paperclip_list_company_memories\|paperclip_create_goal\|paperclip_update_goal\|paperclip_list_goals" server/src/onboarding-assets/default/AGENTS.md | wc -l
# Expected: 8 lines (one per tool)
</verification>

<success_criteria>
All 8 Stapler-specific tools are documented in `server/src/onboarding-assets/default/AGENTS.md` with name, description, and params. New agents automatically receive this documentation because the file is read unchanged at agent creation time.
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-template-propose-bulk/05-01-SUMMARY.md`
</output>
